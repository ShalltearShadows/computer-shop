<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qun.mapper.RoleMapper">

    <select id="getAll" resultType="Role">
        select * from computermall.pe_role
    </select>

    <select id="get" resultType="Role">
        select * from computermall.pe_role where id = #{id}
    </select>

    <insert id="add" parameterType="Role">
        insert into computermall.pe_role(name, perms)
            VALUES(#{name},#{right})
    </insert>

    <delete id="delete">
        delete from computermall.pe_role where id = #{id}
    </delete>

    <update id="update" parameterType="Role">
        update computermall.pe_role
        <set>
            <if test="name != null">
                name = #{name}
            </if>
            <if test="perms != null">
                perms = #{perms}
            </if>
        </set>
        where id = #{id}
    </update>
</mapper>
